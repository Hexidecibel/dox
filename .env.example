# Required: JWT signing secret (generate with: openssl rand -hex 32)
JWT_SECRET=your-secret-here

# Optional: Resend API key for email notifications (invitations, password resets)
# Sign up at https://resend.com to get a key
RESEND_API_KEY=re_xxxx
