// GENERATED by 'npm run build:worker-shared' from shared/releaseReach.ts -- DO NOT EDIT BY HAND.
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// shared/releaseReach.ts
var releaseReach_exports = {};
__export(releaseReach_exports, {
  RELEASE_REACH_COLOR: () => RELEASE_REACH_COLOR,
  RELEASE_REACH_HELP: () => RELEASE_REACH_HELP,
  RELEASE_REACH_LABEL: () => RELEASE_REACH_LABEL,
  RELEASE_REACH_PATTERN: () => RELEASE_REACH_PATTERN,
  RELEASE_REACH_TOKENS: () => RELEASE_REACH_TOKENS,
  hasReachMarker: () => hasReachMarker,
  isReleaseReach: () => isReleaseReach,
  splitReachToken: () => splitReachToken
});
module.exports = __toCommonJS(releaseReach_exports);
var RELEASE_REACH_TOKENS = ["existing", "new-orgs", "config"];
var RELEASE_REACH_LABEL = {
  existing: "Reaches every organisation now",
  "new-orgs": "New organisations only",
  config: "Needs configuration"
};
var RELEASE_REACH_HELP = {
  existing: "Live for every organisation as soon as this release is deployed. Nobody has to do anything.",
  "new-orgs": "A changed default that is applied when an organisation is first set up. Organisations configured before this release keep the configuration they already have \u2014 it is not rewritten, and it will not match the new default.",
  config: "Shipped, but inert until somebody sets it up. Nothing about how your organisation behaves today changes on its own."
};
var RELEASE_REACH_COLOR = {
  existing: "success",
  "new-orgs": "warning",
  config: "info"
};
var RELEASE_REACH_PATTERN = new RegExp(
  `^\\[(${RELEASE_REACH_TOKENS.join("|")})\\]\\s*`
);
function isReleaseReach(value) {
  return typeof value === "string" && RELEASE_REACH_TOKENS.includes(value);
}
function splitReachToken(text) {
  const m = RELEASE_REACH_PATTERN.exec(text);
  if (!m) return { reach: null, rest: text };
  const reach = m[1];
  return isReleaseReach(reach) ? { reach, rest: text.slice(m[0].length) } : { reach: null, rest: text };
}
function hasReachMarker(markdown) {
  const withoutComments = markdown.replace(/<!--[\s\S]*?-->/g, "");
  return withoutComments.split("\n").some((line) => RELEASE_REACH_PATTERN.test(line.replace(/^\s*(?:[-*+]|\d+\.)\s+/, "")));
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  RELEASE_REACH_COLOR,
  RELEASE_REACH_HELP,
  RELEASE_REACH_LABEL,
  RELEASE_REACH_PATTERN,
  RELEASE_REACH_TOKENS,
  hasReachMarker,
  isReleaseReach,
  splitReachToken
});
