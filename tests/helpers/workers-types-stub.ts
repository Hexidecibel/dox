// Stub for @cloudflare/workers-types — provides type-only exports so that
// importing modules that reference Workers types doesn't fail at runtime.
// Actual D1/R2 instances come from Miniflare in test helpers.
export {};
